Coloque aqui o template do certificado.

- Nome esperado: `template.jpg` **ou** `template.png`
- O backend vai inserir o **nome completo** no centro do certificado e gerar um PDF.

Recomendado: `template.jpg` (JPEG), para não depender de bibliotecas extra.

Se nenhum template existir, o backend usa um layout simples (fallback).
